import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.sios.starter',
  appName: 'sios',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
